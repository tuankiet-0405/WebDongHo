<!DOCTYPE html>
<html lang="vi" class="scroll-smooth">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập | CHRONOS</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Sora:wght@300;400;500;600&display=swap"
        rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.iconify.design/iconify-icon/1.0.7/iconify-icon.min.js"></script>
    <style>
        .glass-panel {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
        }
    </style>
    <script>
        tailwind.config = { theme: { extend: { colors: { dark: '#070A12', navy: '#0B1020', gold: '#D6B25E', offwhite: '#EAEAF0', muted: '#9AA3B2' }, fontFamily: { sans: ['Inter', 'sans-serif'], display: ['Sora', 'sans-serif'] } } } }
    </script>
</head>

<body class="bg-dark text-offwhite font-sans antialiased min-h-screen flex items-center justify-center">
    <div class="absolute inset-0 bg-gradient-to-b from-navy to-dark"></div>

    <div class="container mx-auto px-6 relative z-10">
        <div class="max-w-md mx-auto">
            <div class="text-center mb-8">
                <a href="/"
                    class="text-3xl font-display font-semibold tracking-tighter uppercase text-white">CHRONOS</a>
                <h2 class="text-2xl font-display font-medium text-white mt-6 mb-2">Đăng nhập</h2>
                <p class="text-muted">Chào mừng bạn quay lại</p>
            </div>

            <%# Success message %>
                <% if (typeof success_msg !=='undefined' && success_msg && success_msg.length> 0) { %>
                    <div class="bg-green-500/10 border border-green-500/30 rounded-lg p-4 mb-6">
                        <p class="text-green-400 text-sm">
                            <%= success_msg %>
                        </p>
                    </div>
                    <% } %>

                        <%# Validation errors array %>
                            <% if (typeof errors !=='undefined' && errors && errors.length> 0) { %>
                                <div class="bg-red-500/10 border border-red-500/30 rounded-lg p-4 mb-6">
                                    <% errors.forEach(function(e) { %>
                                        <p class="text-red-400 text-sm">
                                            <%= e.msg %>
                                        </p>
                                        <% }); %>
                                </div>
                                <% } %>

                                    <%# Flash error_msg %>
                                        <% if (typeof error_msg !=='undefined' && error_msg && error_msg.length> 0) { %>
                                            <div class="bg-red-500/10 border border-red-500/30 rounded-lg p-4 mb-6">
                                                <p class="text-red-400 text-sm">
                                                    <%= error_msg %>
                                                </p>
                                            </div>
                                            <% } %>

                                                <%# Passport error (this is the key fix!) %>
                                                    <% if (typeof error !=='undefined' && error && error.length> 0) { %>
                                                        <div
                                                            class="bg-red-500/10 border border-red-500/30 rounded-lg p-4 mb-6">
                                                            <p class="text-red-400 text-sm">
                                                                <%= error %>
                                                            </p>
                                                        </div>
                                                        <% } %>

                                                            <form action="/auth/login" method="POST"
                                                                class="glass-panel rounded-2xl p-8">
                                                                <div class="mb-6">
                                                                    <label
                                                                        class="block text-sm text-muted mb-2">Email</label>
                                                                    <div class="relative">
                                                                        <iconify-icon icon="lucide:mail"
                                                                            class="absolute left-4 top-1/2 -translate-y-1/2 text-muted"></iconify-icon>
                                                                        <input type="email" name="email" required
                                                                            placeholder="your@email.com"
                                                                            class="w-full bg-dark border border-white/10 rounded-lg pl-12 pr-4 py-3 text-white focus:border-gold outline-none">
                                                                    </div>
                                                                </div>

                                                                <div class="mb-6">
                                                                    <label class="block text-sm text-muted mb-2">Mật
                                                                        khẩu</label>
                                                                    <div class="relative">
                                                                        <iconify-icon icon="lucide:lock"
                                                                            class="absolute left-4 top-1/2 -translate-y-1/2 text-muted"></iconify-icon>
                                                                        <input type="password" name="password" required
                                                                            placeholder="••••••••"
                                                                            class="w-full bg-dark border border-white/10 rounded-lg pl-12 pr-4 py-3 text-white focus:border-gold outline-none">
                                                                    </div>
                                                                </div>

                                                                <div class="flex justify-between items-center mb-6">
                                                                    <label
                                                                        class="flex items-center gap-2 cursor-pointer">
                                                                        <input type="checkbox" name="remember"
                                                                            class="w-4 h-4 rounded border-white/20 bg-dark text-gold">
                                                                        <span class="text-sm text-muted">Ghi nhớ</span>
                                                                    </label>
                                                                    <a href="/auth/forgot-password"
                                                                        class="text-sm text-gold hover:underline">Quên
                                                                        mật
                                                                        khẩu?</a>
                                                                </div>

                                                                <button type="submit"
                                                                    class="w-full bg-gold hover:bg-[#C5A354] text-dark font-bold py-4 rounded-lg text-sm uppercase tracking-wide flex items-center justify-center gap-2">
                                                                    <iconify-icon
                                                                        icon="lucide:log-in"></iconify-icon>Đăng nhập
                                                                </button>
                                                            </form>

                                                            <p class="text-center text-muted mt-6">Chưa có tài khoản? <a
                                                                    href="/auth/register"
                                                                    class="text-gold hover:underline font-medium">Đăng
                                                                    ký ngay</a></p>
        </div>
    </div>
</body>

</html>